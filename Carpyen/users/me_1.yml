info:
  name: me_1
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{url}}/users/me"
  body:
    type: json
    data: |-
      {
          "address": "Avenida Equipetrol 100, Santa Cruz, Bolivia 1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
