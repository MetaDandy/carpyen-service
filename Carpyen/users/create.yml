info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/users"
  body:
    type: json
    data: |-
      {
          "name": "probando", 
          "email": "probando@gmail.com",
          "password": "12345678",
          "confirm_password": "12345678",
          "role": "DESIGNER"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
