info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/clients/login"
  body:
    type: json
    data: |
      {
          "email": "juan.palo@gmail.com",
          "password": "cliente123"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: "let json = res.getBody();\r

        bru.setEnvVar(\"authToken\", json.token);"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
