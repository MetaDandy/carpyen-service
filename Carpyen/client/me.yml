info:
  name: me
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/clients/me"
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
