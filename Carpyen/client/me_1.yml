info:
  name: me_1
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{url}}/clients/me"
  body:
    type: json
    data: "{\r

      \    \"address\": \"Avenida plan 3000, plan 3000, Santa Cruz\"\r

      \r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
