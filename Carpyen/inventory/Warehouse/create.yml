info:
  name: /create
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/warehouses/"
  body:
    type: json
    data: |
      {
        "name": "almacen prueba",
        "address": "calle pollito",
         "description": "es un almacen",
         "phone":"74015468"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
