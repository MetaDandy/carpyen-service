info:
  name: material-types
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/extra-information/material-types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
